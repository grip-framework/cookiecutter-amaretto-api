# `{{cookiecutter.module_slug}}Web` keeps the controllers and views which
# define your web interface.
#
# `{{cookiecutter.module_slug}}Web` is also responsible for housing the middleware
# defined using the pipes base class.
module {{cookiecutter.module_slug}}Web; end